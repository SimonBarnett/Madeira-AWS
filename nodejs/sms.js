# sms.js

## Overview
TextMagic SMS client wrapper.

## Purpose
- Send OTPs and PINs via SMS
- Normalize phone numbers (UK-focused)

## Key Function
- `sendSmsTextmagic(phone, message)`

## Configuration
Loads credentials from SSM via the config layer.

---
*Part of the hierarchical documentation on the `feature/documentation` branch.*